export default function Notifications(){
    return(
        <> Notif </>
    )

    // shift expiry notifications for hr managers 
    // missed punch alerts for employees, department heads and payroll
    // notify employees of assigned shifts
    // notify hr of repeated lateness
}